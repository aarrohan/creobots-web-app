import type { Metadata } from "next";
import Content from "./Content";

export const metadata: Metadata = {
  title: "Subscription - Creobots",
};

export default function Subscription() {
  return <Content />;
}
